# --- Large data inputs (fetched with dl.sh, ~5 GB) ---
mc/
*.root

# NOTE: filelists/*.txt are intentionally tracked — they hold absolute /blue
# paths to the shared HPG inputs and are meant to be shared. Rebuild with
# make_filelist.sh if the mc/ contents change.

# --- Build artifacts ---
create_fourlepton_ntuple
create_dilepton_ntuple
*.o
*.so
*.d

# --- SLURM logs / scratch ---
logs/
.filelist_clean.txt

# --- Editor / OS cruft ---
*.swp
*~
.DS_Store
